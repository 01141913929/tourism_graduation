import 'package:flutter/material.dart';

/// ألوان لوحة Super Admin
class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(0xFF1A5F52); // أخضر فرعوني
  static const Color primaryLight = Color(0xFF2A8F7A);
  static const Color secondary = Color(0xFFD4A574); // ذهبي
  static const Color accent = Color(0xFFE8442E); // أحمر

  // ألوان الخلفية
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color sidebarBg = Color(0xFF1A1A2E); // للـ Sidebar
  static const Color cardBackground = Color(0xFFFFFFFF);

  // ألوان النص
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);
  static const Color textOnDark = Color(0xFFFFFFFF);

  // ألوان الحالة
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ألوان إضافية
  static const Color divider = Color(0xFFE5E7EB);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // ألوان حالات الطلبات
  static const Color pending = Color(0xFFF59E0B);
  static const Color approved = Color(0xFF10B981);
  static const Color rejected = Color(0xFFEF4444);
}
